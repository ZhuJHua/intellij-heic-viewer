<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<!-- <description> and <change-notes> are generated by Gradle (patchPluginXml): the description from the
     "Plugin description" section of README.md, the change notes from CHANGELOG.md. <version> and <idea-version>
     come from gradle.properties. -->
<idea-plugin>
  <id>cn.yooss.heic-viewer</id>
  <name>HEIC Viewer</name>
  <vendor url="https://yooss.cn" email="livejinghua@gmail.com">ZhuJHua</vendor>

  <depends>com.intellij.modules.platform</depends>
  <depends>com.intellij.platform.images</depends>
  <!-- Everything this plugin contributes lives in heic-viewer-macos.xml and is only loaded on macOS: the decoder
       needs the system's ImageIO.framework, and on other systems .heic must not be mapped to the Image file type
       without a reader. (An optional dependency rather than a mandatory one because Android Studio's
       product-info.json does not declare the com.intellij.modules.os.mac alias, so Plugin Verifier cannot resolve
       it; the IDE itself provides it at runtime on macOS.) -->
  <depends optional="true" config-file="heic-viewer-macos.xml">com.intellij.modules.os.mac</depends>

  <resource-bundle>messages.HeicBundle</resource-bundle>
</idea-plugin>
